<%= form_with(model: [:admin, subscription]) do |form| %>
  <% if subscription.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subscription.errors.count, "error") %>. Подписка не можеть быть сохранена:</h2>

      <ul>
        <% subscription.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :email, style: "display: none" %>
    <%= form.email_field :email %>
  </div>

  <div>
    <%= form.submit :"Подписаться" %>
  </div>
<% end %>
