<div id="<%= dom_id post %>">

  <h5>
    <%= post.category.name %>
  </h5>

  <%= post.user.email %>

  <h2>
    <%= post.title%>
  </h2>

  <%= image_tag post.post_image.url, width: 300 if post.post_image.url %>

  <p>
    <%= post.description %>
  </p>

</div>
