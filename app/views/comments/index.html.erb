<p style="color: green"><%= notice %></p>

<h1>Комментарии</h1>

<%= link_to "Новый комментарий", new_comment_path %>

<div id="comments">
  <% @comments.each do |comment| %>
      <%= link_to comment, class: 'commentCard' do %>
        <%= render comment %>
      <% end %>
  <% end %>
</div>
